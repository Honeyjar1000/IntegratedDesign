from ultralytics import YOLO
import torch

# Load model
# model = YOLO("yolov8n.pt")

# Train the model
# results = model.train(data="coco8.yaml", epochs=100, imgsz=640) # NOTE: training will also save best weights in some default folder

# Save model
# torch.save(model.model, "vision/models/yolov8n.pt")
